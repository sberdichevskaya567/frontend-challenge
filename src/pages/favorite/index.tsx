import type { JSX } from 'react';
import FavoriteList from '../../feature/favorite/favorite-list';

const FavoritePage = (): JSX.Element => {
  return <FavoriteList />;
};

export default FavoritePage;
